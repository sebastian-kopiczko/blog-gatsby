import React from "react"

const Icon = ({ title }) => {
  return (
    <img
      style={{ marginRight: "10px" }}
      src="https://via.placeholder.com/15x15"
      alt={title}
    />
  )
}

export default Icon
