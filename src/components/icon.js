import React from "react"

const Icon = props => {
  return (
    <img
      style={{ marginRight: "10px" }}
      src="https://via.placeholder.com/15x15"
      alt={props.title}
    />
  )
}

export default Icon
